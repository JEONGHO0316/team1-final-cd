apiVersion: apps/v1
kind: Deployment
metadata:
  name: petclinic-was
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: petclinic
        image: petclinic-image-placeholder  # 👈 여기가 핵심! 나중에 갈아끼움
        ports:
        - containerPort: 8080
